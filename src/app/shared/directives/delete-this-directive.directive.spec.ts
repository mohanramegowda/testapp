import { DeleteThisDirectiveDirective } from './delete-this-directive.directive';

describe('DeleteThisDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteThisDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
